import React from "react"
import ReactDOM from "react-dom"
import Manager from './Manager'
const app = document.getElementById("app");

// ReactDOM.render(<Counter store={TodoStore} />, app)
ReactDOM.render(<Manager />, app)